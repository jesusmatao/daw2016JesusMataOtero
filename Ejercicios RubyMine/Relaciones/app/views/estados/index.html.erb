<h1>Listando estados</h1>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Pais</th>
      <th>Acciones</th>
    </tr>
  <thead/>
  <tbody>
    <% @estados.each do |estado| %>
    <tr>
      <td><%= estado.nombre %></td>
      <td><%= estado.pais %></td>
      <td>
      <td><%= link_to 'Mostrar', estado %></td>
      <td><%= link_to 'Editar', edit_estado_path(estado) %></td>
      <td><%= link_to 'Eliminar', estado, method: :delete, data: { confirm: '¿Esta seguro de eliminar este Genero?' } %></td>
      </td>
    </tr>
      <% end %>
  </tbody>
</table>